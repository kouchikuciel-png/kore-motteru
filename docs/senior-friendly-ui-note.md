# 高齢者向けUI方針

家主画面では、利用者が技術用語を理解していることを前提にしない。

- 内部判定ではISBNを使ってよい
- 画面では原則として「バーコード」「本の番号」など行動に直結する言葉を使う
- 誤った書籍バーコードを読んだ場合は「別のバーコードです」と表示する
- その場で正しい規格名を教えず、「近くにある、もう一つのバーコードをカメラに向けてください」と次の行動を案内する
- 誤読・不採用時は赤、正常読取時は肯定色を使う

技術知識ではなく、画面の指示だけで次の操作へ進めることを受入基準とする。
